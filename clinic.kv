#:import dp kivy.metrics.dp

WindowManager:
    Dashboard:
    AddVisit:
    Search:
    Debts:
    PDF:


<MainButton@Button>:
    size_hint_y: None
    height: dp(50)
    background_color: (0.2, 0.6, 0.9, 1)
    bold: True
    font_size: 16


<Dashboard>:
    name: "dashboard"

    BoxLayout:
        orientation: "vertical"
        padding: dp(15)
        spacing: dp(15)

        Label:
            text: "نظام إدارة العيادة"
            font_size: 22
            bold: True
            size_hint_y: None
            height: dp(50)

        Label:
            id: stats
            text: ""
            font_size: 16

        MainButton:
            text: "➕ إضافة زيارة"
            on_press: app.root.current = "add"

        MainButton:
            text: "🔍 بحث"
            on_press: app.root.current = "search"

        MainButton:
            text: "💰 الديون"
            on_press: app.root.current = "debts"

        MainButton:
            text: "📄 تصدير PDF"
            on_press: app.root.current = "pdf"



<AddVisit>:
    name: "add"

    BoxLayout:
        orientation: "vertical"
        padding: dp(15)
        spacing: dp(10)

        TextInput:
            id: name
            hint_text: "اسم المريض"
            size_hint_y: None
            height: dp(50)

        TextInput:
            id: price
            hint_text: "سعر الكشف"
            input_filter: "float"
            size_hint_y: None
            height: dp(50)

        TextInput:
            id: paid
            hint_text: "المبلغ المدفوع"
            input_filter: "float"
            size_hint_y: None
            height: dp(50)

        MainButton:
            text: "💾 حفظ"
            on_press: root.save()

        Label:
            id: result
            text: ""
            font_size: 16

        MainButton:
            text: "⬅ رجوع"
            on_press: app.root.current = "dashboard"



<Search>:
    name: "search"

    BoxLayout:
        orientation: "vertical"
        padding: dp(15)
        spacing: dp(10)

        TextInput:
            id: search_name
            hint_text: "اكتب اسم المريض"
            size_hint_y: None
            height: dp(50)

        MainButton:
            text: "🔎 بحث"
            on_press: root.search()

        ScrollView:
            Label:
                id: results
                text: ""
                size_hint_y: None
                text_size: self.width, None
                height: self.texture_size[1]

        MainButton:
            text: "⬅ رجوع"
            on_press: app.root.current = "dashboard"



<Debts>:
    name: "debts"

    BoxLayout:
        orientation: "vertical"
        padding: dp(15)
        spacing: dp(10)

        Label:
            text: "المرضى الذين عليهم ديون"
            font_size: 18
            bold: True
            size_hint_y: None
            height: dp(40)

        ScrollView:
            Label:
                id: debts_label
                text: ""
                size_hint_y: None
                text_size: self.width, None
                height: self.texture_size[1]

        MainButton:
            text: "⬅ رجوع"
            on_press: app.root.current = "dashboard"



<PDF>:
    name: "pdf"

    BoxLayout:
        orientation: "vertical"
        padding: dp(15)
        spacing: dp(15)

        Label:
            text: "تصدير تقرير PDF كامل"
            font_size: 18
            bold: True

        MainButton:
            text: "📄 إنشاء التقرير"
            on_press: root.generate()

        Label:
            id: msg
            text: ""

        MainButton:
            text: "⬅ رجوع"
            on_press: app.root.current = "dashboard"
